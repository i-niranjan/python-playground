Each object has its own sets of properties/features thats what we called namespaces

Static method :

Static method is smthn which are not dependent on object , just use decorater @staticmethod

so in summary if you're passing self in methods its a instance method

if adding decorator of @staticmethods then its a static method, if you added @classmethod decorator then its a classicmethod
