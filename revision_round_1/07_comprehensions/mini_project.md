# Mini Project: Data Cleaner

You are given messy user data.

Create a script that:

- Converts names to title case
- Keeps only active users
- Extracts emails into a list
- Creates a dictionary of user id to user name
- Removes duplicate tags from all users

Use comprehensions where they make the code clearer.

